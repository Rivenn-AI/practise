Git versionlarni urganamiz:

```
git config --global user.name "your name"
git config --global user.email "your email"
git init

git status
git commit -m "your commite"
git push origin master
git pull origin msxter
```
